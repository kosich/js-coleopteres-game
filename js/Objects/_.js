( function ( global ) {
    'use strict';

    global.Objects = {
        BasicObject: _inherit( Basic, {
            _namespace: 'Objects',
            _type: 'BasicObject'
        } )
    };

} )( this );
